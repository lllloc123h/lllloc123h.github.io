{
  "name": "appthongkefrontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rimraf dist && rimraf node_modules && npm install",
    "build": "vite build",
    "preview": "vite preview",
    "build-deploy": "npm run clean && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "axois": "^0.0.1-security",
    "bootstrap": "^5.3.6",
    "bootstrap-icons": "^1.13.1",
    "vue": "^3.5.13",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "gh-pages": "^6.3.0",
    "rimraf": "^6.0.1",
    "vite": "^6.3.5",
    "vite-plugin-mpa": "^1.2.0"
  }
}
